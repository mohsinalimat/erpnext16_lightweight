# Pin the Docker Compose project name so it stays stable even if this folder is
# renamed or moved. Keeping it explicit here means `docker compose` always manages
# the same containers, network, and the "erpnext16_lightweight_erpnext-data" volume,
# regardless of the current folder name. Do not delete this file.
COMPOSE_PROJECT_NAME=erpnext16_lightweight
